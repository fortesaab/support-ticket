services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: support_ticket_app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - .:/var/www
    networks:
      - support_ticket_network

  nginx:
    image: nginx:alpine
    container_name: support_ticket_nginx
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - .:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - support_ticket_network

  mysql:
    image: mysql:8.0
    container_name: support_ticket_mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: support_ticket
      MYSQL_ROOT_PASSWORD: root
      MYSQL_PASSWORD: secret
      MYSQL_USER: laravel
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - support_ticket_network

  redis:
    image: redis:7-alpine
    container_name: support_ticket_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - support_ticket_network

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: support_ticket_phpmyadmin
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      PMA_HOST: mysql
      PMA_USER: laravel
      PMA_PASSWORD: secret
    networks:
      - support_ticket_network

networks:
  support_ticket_network:
    driver: bridge

volumes:
  mysql_data:
    driver: local
